#include <stdio.h>

int main() {
    int n, i = 1;

    printf("Enter a positive integer n: ");
    scanf("%d", &n);

    printf("The first %d even numbers are: ", n);

    while (i <= n) {
        printf("%d ", i * 2);
        i++;
    }

    printf("\n");

    return 0;
}
